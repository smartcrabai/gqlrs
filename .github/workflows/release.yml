name: Release
on:
  push:
    branches:
      - release
    paths:
      - "**/Cargo.toml"
      - ".github/workflows/release.yml"

jobs:
  publish:
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        package:
          - name: gqlrs-value
            registryName: gqlrs-value
            path: value
          - name: gqlrs-parser
            registryName: gqlrs-parser
            path: parser
          - name: gqlrs-derive
            registryName: gqlrs-derive
            path: derive
          - name: gqlrs
            registryName: gqlrs
            path: .
          - name: gqlrs-extras
            registryName: gqlrs-extras
            path: extras
          - name: gqlrs-actix-web
            registryName: gqlrs-actix-web
            path: integrations/actix-web
          - name: gqlrs-warp
            registryName: gqlrs-warp
            path: integrations/warp
          - name: gqlrs-poem
            registryName: gqlrs-poem
            path: integrations/poem
          - name: gqlrs-axum
            registryName: gqlrs-axum
            path: integrations/axum
          - name: gqlrs-rocket
            registryName: gqlrs-rocket
            path: integrations/rocket
    steps:
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
      - name: Checkout
        uses: actions/checkout@v4
      - name: get version
        working-directory: ${{ matrix.package.path }}
        run: echo PACKAGE_VERSION=$(sed -nE 's/^\s*version = "(.*?)"/\1/p' Cargo.toml) >> $GITHUB_ENV
      - name: check published version
        run: echo PUBLISHED_VERSION=$(cargo search ${{ matrix.package.registryName }} --limit 1 | sed -nE 's/^[^"]*"//; s/".*//1p' -) >> $GITHUB_ENV
      - name: cargo login
        if: env.PACKAGE_VERSION != env.PUBLISHED_VERSION
        run: cargo login ${{ secrets.CRATES_TOKEN }}
      - name: cargo package
        if: env.PACKAGE_VERSION != env.PUBLISHED_VERSION
        working-directory: ${{ matrix.package.path }}
        run: |
          cargo package
          echo "We will publish:" $PACKAGE_VERSION
          echo "This is current latest:" $PUBLISHED_VERSION
      - name: Publish ${{ matrix.package.name }}
        if: env.PACKAGE_VERSION != env.PUBLISHED_VERSION
        working-directory: ${{ matrix.package.path }}
        run: |
          echo "# Cargo Publish"
          cargo publish --no-verify
